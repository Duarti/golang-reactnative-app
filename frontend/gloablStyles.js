import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    text: {
        fontSize: 15,
        fontWeight: 600,
    },
    title: {
        fontWeight: 'bold',
        fontSize: 25,
        padding: 10,
        textAlign: 'center',
    },
    container: {
        padding: 20,
        alignItems: 'center',
    }
})